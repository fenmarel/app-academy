<h1>All Users</h1>

<ul>
  <% @users.each do |user| %>
    <li>
      <%= link_to user.username, user_url(user) %>
      <% if Friendship.can_friend?(current_user, user) %>
        <%= button_to "Follow", user_friendships_url(user) %>
      <% end %>
    </li>
  <% end %>
</ul>