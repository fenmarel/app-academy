<h1><%= @user.username %></h1>

<h2>Secrets</h2>
<ul id="secret-listing">
  <% @user.received_secrets.each do |secret| %>
    <li><%= "#{secret.title}" %></li>
  <% end %>
</ul>

<div id="secret-form-container">
  <%= render 'secrets/form' %>
</div>